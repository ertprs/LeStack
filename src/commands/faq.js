const execute = (leStack, msg, args) => {
  msg.reply("Em construção, baby, em contrução...")
}

module.exports = {
  name: "faq",
  help: 'Commandos de pergunta e resposta',
  about: '!faq question -> Cria uma pergunta\n!faq response -> Responde a pergunta',
  execute
}